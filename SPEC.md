# バスデータ移行ツール 仕様書

## 1. ツール概要・対象範囲

「駅すぱあと API」のバスデータ再編に伴い、利用者が保持する旧データを新データへ移行する作業を補助するCLIツールを提供する。

本書はツールの動作を説明する。特定の再編・利用者におけるデータ抽出、確認、反映などの業務手順は、対象の再編に対して配布されるデータ移行手順書を参照する。

| 機能                       | 対象データ                        | 結果ファイル                                | API利用                |
| -------------------------- | --------------------------------- | ------------------------------------------- | ---------------------- |
| バス停データ変換           | バス停コード・バス停名称          | `変換結果-バスデータ.csv`                   | 不要（オフライン変換） |
| 経路シリアライズデータ移行 | `Course/SerializeData`            | `移行先経路候補-経路シリアライズデータ.csv` | 移行元API・移行先API   |
| 定期経路文字列移行         | `assignDetailRoute`/`assignRoute` | `変換結果-定期経路文字列.csv`               | 移行先API              |

本ツールは、次の条件を満たす同種のバスデータ移行を対象とする。

- 旧バス停と新バス停を対応付けるCSVが提供される
- APIを使用する機能では、本仕様書が利用するエンドポイント、パラメーター、レスポンス項目に互換性がある
- 移行元・移行先APIの接続先と適用開始日を移行プロファイルで指定できる
- 経路シリアライズデータ移行では、移行元APIで旧経路を再現できる

利用者のシステムへの結果反映、移行先候補の業務上の妥当性判断、対応表に無い移行先の推測、異なるAPI仕様への自動適応は対象外とする。

### 1.1 現在対応していないこと

- **PassStatus切替後の定期代は比較できない（経路シリアライズデータ移行）**: 旧経路シリアライズデータが `PassStatus` の切替（複数ある定期券種別からの選択）後の状態を保持していても、本ツールは新経路の探索を既定の `PassStatus` で行い、同じ選択状態を再現しない。そのため `route_changed`・`fare_changed` が `変化なし` でも、切替後の条件で使う定期代（`Teiki*`）は旧新で異なっている可能性があり、`teiki_changed` はこれを検知できないことがある。この場合の定期代確認は対象外とし、必要であれば利用者が別途確認する（差分の算出基準は3.2「候補ごとの差分」を参照）
- **平均探索は1候補しか確認しない（定期経路文字列移行）**: 平均探索（処理7）は `answerCount=1` で実行しており、同じ発着駅間に複数の経路が存在する場合でも1件しか取得しない。取得した1件の路線・方向が元の定期経路文字列と異なれば `route_changed` は `変化あり` になるが、他候補の探索・提示は行わない。そのため取得した1件が元と一致しない場合、正しい移行先を見つけられないことがある。経路シリアライズデータ移行（3.2）と同様に複数候補を取得・提示する方式への変更を検討中（処理は3.3「処理」の6〜10、差分は「移行前後の差分」を参照）

## 2. 共通仕様

各機能で同じ対応表・CSV入出力・ステータス・移行プロファイルを扱うため、機能間で解釈と動作がずれないよう共通仕様として定義する。

### 2.1 対応表（CSV）

配布される対応表CSV（例:「新旧バスデータ対応表\_{バージョン}.csv」）をそのまま読み込む。列名の変更・整形は不要。

**読み込み**

- 認識する列は、配布される対応表の「旧コード」「新コード」「旧バス停名(フル)」「新バス停名(フル)」。列の並び順は問わず、他の列は無視する
- 列名は大文字小文字・前後空白を無視して照合する
- 先頭のBOMは読み飛ばす
- 旧コード/新コードが空の行はスキップする

**重複の扱い**

- 旧名称の同名重複は候補として保持する。ただし移行先（新コード）が同じ重複は一意として扱う（複数の旧バス停が1つの新バス停へ統合された場合が該当し、移行先は決まっているため）
- 同じ旧コードに異なる移行先が複数ある場合も候補として保持し、該当データは `要確認（同名バス停あり）` とする。読み込み時に画面へ警告を出す

**自動検出（`--mapping` 省略時）**

- 検出の対象は、名称列（`旧バス停名(フル)`/`新バス停名(フル)`）も持つCSVのみとする
- 結果ファイル（タイムスタンプ付きの退避ファイルを含む）は、ファイル名で対応表候補から除外する。結果CSVは再実行時の入力として使うものであり、対応表としては使わない
- 候補が複数見つかった場合は自動選択せず、候補を画面に表示してエラー終了する。利用者は `--mapping` で使用する対応表を指定する

### 2.2 入出力CSV

**ファイル形式**

- ヘッダー行必須。列名は大文字小文字・前後空白を無視する
- ヘッダーの列数を超える値がある行はエラー終了する（末尾の空欄は許容する）。値に含まれるコンマのクォート漏れで列がずれた入力を、黙って切り捨てて処理しないため
- 入力はUTF-8（BOM付き・なし両対応）。UTF-8として解釈できない場合は、Shift_JIS の可能性がある旨を示してエラー終了する
- 出力はUTF-8 BOM付きとする

**入力の列**

機能ごとに必要な列は3章で定義する。ファイル単位では次の条件を満たすこと。

| 条件                                                                          | 満たさない場合                                                             |
| ----------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `old_code`・`old_name`・`serialize_data`・`detail_route` のいずれかの列がある | 処理を開始せずエラー終了する                                               |
| `detail_route` に値がある場合、`origin`・`destination`・`date` の列もある     | そのファイルの定期経路文字列だけを未処理とし、他の機能・ファイルは継続する |

- `id` は任意とする。指定した値は結果CSVへそのまま出力する。省略時は結果CSVの `id` を空欄とするため、複数ファイルや複数候補を扱う場合は入力と結果の対応が分かりづらくなる。結果の確認を容易にするため指定を推奨する
- 1つのCSVに異なる種類の行を混在できるが、1行には1種類の移行データだけを指定する
- `date`/`time` の意味は機能ごとに異なる。経路シリアライズデータ移行では元の探索日時、定期経路文字列移行では移行先APIで再探索・動作確認する日時とする

**結果ファイル**

- 機能ごとの固定名（1章の表）で、最初の入力ファイルと同じフォルダーに作成する
- 同名の結果ファイルがある場合は、タイムスタンプを付けて退避してから書く（形式: `変換結果-バスデータ-YYYYMMDD-HHMMSS.csv`）
- 入力の全行を結果に残す。どの機能の対象にもならない行も `エラー` として出力する
- 各機能で定義した入力列を含める（`status` が `エラー` の行を抜き出して再実行できる）

### 2.3 ステータス・差分の値

結果CSVと画面にそのまま表示するため日本語とする。

| 値                         | 意味                                             | 対象機能                             |
| -------------------------- | ------------------------------------------------ | ------------------------------------ |
| `変換済み`                 | 新データの生成・確認まで成功                     | バス停データ変換、定期経路文字列移行 |
| `移行先の候補`             | 移行先の候補。利用者による選択が必要             | 経路シリアライズデータ移行           |
| `対応表に該当なし`         | 対応表に該当なし。自動変換しない                 | 全機能                               |
| `要確認（同名バス停あり）` | 同名または複数の移行先があり、一意に決められない | 全機能                               |
| `更新不要`                 | 入力した定期経路文字列が移行先環境で有効         | 定期経路文字列移行                   |
| `エラー`                   | 処理失敗。理由は `detail` に出力                 | 全機能                               |

- 差分列（`route_changed`/`fare_changed`）の値は `変化あり`/`変化なし`。判定できない場合は空。`route_changed` は経路を扱う2機能、`fare_changed` は経路シリアライズデータ移行のみ

### 2.4 移行プロファイル（JSON）

再編ごとに変わる値は `--config` で指定するJSONへ分離する。ツール本体は、特定の接続先・適用開始日を既定値として持たない。

`source_api` は旧経路を再現する移行元API、`target_api` は新データを生成・確認する移行先APIを表す。

| キー                     | 説明                                         | 必要な機能                 |
| ------------------------ | -------------------------------------------- | -------------------------- |
| `profile_id`             | 移行プロファイルを一意に識別する名称         | APIを使う全機能            |
| `source_api_base_url`    | 旧経路を再現する移行元APIのベースURL         | 経路シリアライズデータ移行 |
| `target_api_base_url`    | 新データを生成・確認する移行先APIのベースURL | APIを使う全機能            |
| `target_data_start_date` | 移行先データの適用開始日。`YYYYMMDD`形式     | 定期経路文字列移行         |

- URLは `http://` または `https://` から始まる有効な形式とする
- `target_data_start_date` は実在する日付とする
- 値はすべてJSON文字列とする
- 不明なキーは入力ミスを検出するためエラーとする
- APIを使う入力がある場合、必要なキーの不足した機能は実行せず、他の実行可能な機能は継続する
- 実行開始時に `profile_id` を表示する

## 3. 機能仕様

### 3.1 バス停データ変換

対応表だけで完結する変換。旧コードまたは旧名称で照合し、新コード・新名称を出力する。

| 項目   | 内容                                                                                |
| ------ | ----------------------------------------------------------------------------------- |
| 入力列 | `id`, `old_code`, `old_name`（必須: `old_code`/`old_name` のいずれか。`id` は任意） |
| 出力列 | `id, status, detail, old_code, old_name, new_code, new_name`                        |
| API    | 呼び出しなし                                                                        |

**照合**

1. `old_code` あり → 旧コードで照合する。名称へのフォールバックはしない
2. `old_code` なし → `old_name` で照合する

| 照合の結果                                                            | ステータス                 | 出力                                |
| --------------------------------------------------------------------- | -------------------------- | ----------------------------------- |
| 1件一致                                                               | `変換済み`                 | `new_code`/`new_name`               |
| 名称が複数一致、または同じ旧コードに複数の移行先がある                | `要確認（同名バス停あり）` | なし                                |
| 不一致                                                                | `対応表に該当なし`         | なし                                |
| `old_code`・`old_name`・`serialize_data`・`detail_route` がいずれも空 | `エラー`                   | バス停データの結果ファイルへ1行出す |

### 3.2 経路シリアライズデータ移行

移行元APIで旧経路を再現し、駅を新コードへ変換して移行先APIで再探索する。動作確認できた経路を候補として提示し、どれを採用するかは利用者が判断する。移行後も使用するデータだけを入力対象とする。

| 項目   | 内容                                                                                                                                                                                                                           |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 入力列 | `id`, `serialize_data`, `via_list`, `date`, `time`, `search_type`, `condition_detail`（必須: `serialize_data`。`id` は任意）                                                                                                   |
| 出力列 | `id, candidate_no, status, detail, serialize_data, via_list, date, time, search_type, condition_detail, new_serialize_data, route_changed, old_route, new_route, fare_changed, old_fare, new_fare, old_time_min, new_time_min` |
| API    | 移行元API（旧経路の再現）、移行先API（再探索・動作確認）                                                                                                                                                                       |

#### 処理

1. 移行元APIの `course/edit`（`checkEngineVersion=false`）で旧経路を再現する
2. 経路に旧コードが無い → `対応表に該当なし`
3. 駅を新コードへ変換し、再探索する（条件は「再探索の条件」を参照）
4. 探索結果を `answerCount` 件（`--candidates`、既定3件）取得し、移行先候補とする
5. 候補ごとに移行先APIの `course/edit`（`checkEngineVersion=true`）で動作確認する
6. 動作確認に失敗した候補・旧コードが残存する候補は、その候補行を `エラー` とする。動作確認できた候補は `移行先の候補`
7. 旧経路と各候補を比較し、差分を出力する（「候補ごとの差分」を参照）
8. 採用可能な候補が1件も無ければ行全体を `エラー`、1件以上あれば `移行先の候補`

#### 再探索の条件

| 入力            | 変換する駅                                                    | 探索条件                                                                                                                                                                                                                   |
| --------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `via_list` あり | `via_list` の各要素（旧コード・旧バス停名の両方で対応表と照合） | `viaList` 以外の条件は入力値をそのまま使い、条件を追加しない                                                                                                                                                               |
| `via_list` なし | 旧経路の先頭・末尾・経由駅                                    | `sort=ekispert`（駅すぱあと探索順）を既定とし、`dataType` に応じて設定する（`onTimetable`: `date`/`time`=実出発日時（`DepartureState.Datetime.text`）・`searchType=departure` / `plain`: `searchType=plain`・`time` なし） |

- 移行先が一意に決められない駅・要素があれば `要確認（同名バス停あり）` とし、再探索しない
- 元の探索条件は `SerializeData` から復元できないため、`sort` は既定値を明示する

#### 候補ごとの差分

| 列                              | 内容                                                        |
| ------------------------------- | ----------------------------------------------------------- |
| `old_route` / `new_route`       | 経路を「駅名 →[路線名]→ 駅名」形式に要約した文字列      |
| `route_changed`                 | 経由駅または利用路線に実質的な変化があれば `変化あり`     |
| `old_fare` / `new_fare`         | 片道の運賃(`Fare`)と料金(`Charge`)の合計（円）              |
| `fare_changed`                  | 上記金額が変わっていれば `変化あり`（取得できない場合は空） |
| `old_time_min` / `new_time_min` | 所要時間（`timeOnBoard`+`timeWalk`+`timeOther`、分）        |

- **経路の比較基準**: 駅は旧経路の駅コードを対応表で変換した結果と新経路の駅コードを比較する。路線は系統名で比較し、会社名部分だけの改称を経路変更と判定しない
- 旧経路に移行先が一意に決められない駅を含む場合、駅の比較はできないため `route_changed` を空とし、判定できない旨を `detail` に記録する
- **金額の算出基準**: 運賃・料金それぞれについて `FareSummary`/`ChargeSummary` があればその `Oneway` を、無ければ合計計算に含まれる区間（`selected=true`）の `Oneway` を合算し、2種別の合計を金額とする。定期券(`Teiki*`)は含めない
- 差分があること自体では `エラー` としない。`変化あり` の理由は `detail` に記録する

#### 出力形式

- 1候補=1行。`candidate_no` で区別する。候補を出す前に確定した行（`対応表に該当なし`・`エラー`）は1行のみで `candidate_no` は空。入力列は候補行にも繰り返し出力する
- 候補行の `エラー` は結果CSVにのみ出力する。画面の要確認行・ステータス別件数・終了コードは入力1行を単位として判定し、候補行は数えない。採用可能な候補が1件でもあれば、その入力は処理できたものとして扱う

### 3.3 定期経路文字列移行

入力の定期経路文字列が移行先環境で有効かを確認し、無効なものはバス停名を変換して組み立て直す。候補は複数出さず1件を返す。

| 項目   | 内容                                                                                                                                              |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 入力列 | `id`, `origin`, `destination`, `date`, `time`, `route_type`, `detail_route`（必須: `origin`, `destination`, `date`, `detail_route`。`id` は任意） |
| 出力列 | `id, status, detail, origin, destination, date, time, route_type, detail_route, new_detail_route, route_changed, old_route, new_route`            |
| API    | 移行先API（割り当て確認・平均探索・動作確認）                                                                                                     |

- `origin`/`destination` は移行先環境で有効な駅コード（新コード）を前提とする。割り当て確認（処理4）にのみ使い、平均探索（処理7）の `viaList` には使わない
- `route_type` は、`detail_route` を移行先APIへ渡すときのパラメーター名を指定する。割り当て確認（処理4）と動作確認（処理10）では、この名前のパラメーターへ定期経路文字列を渡す
- `assignDetailRoute` は方向を含む形式（`バス停:路線:方向:…:バス停`）、`assignRoute` は方向を含まない形式（`バス停:路線:…:バス停`）を表す。形式検証（処理2）と組み立て直し（処理9）も指定した形式に合わせる
- 空欄は `assignDetailRoute` とし、それ以外の値は `エラー` とする

#### 処理

1. `date` を検証する。`YYYYMMDD` 形式の実在する日付でなければ `エラー`。移行プロファイルの `target_data_start_date` 未満なら `エラー`（形式の誤りと適用開始日の誤りは `detail` で区別する）
2. 定期経路文字列の形式を検証する（基準は「形式検証」を参照）。満たさなければ `エラー`
3. 含まれるバス停名が対応表の旧名称にひとつも一致しなければ `対応表に該当なし`（APIを呼ばない）
4. 移行先APIで割り当て確認する（`route_type` のパラメーター+`addAssignStatus=true`）
5. `AssignStatus.code=0` かつ `requireUpdate=0` → `更新不要`
6. 定期経路文字列を分解し、バス停名を対応表の新名称・新コードへ変換する。一意に決められない場合は `要確認（同名バス停あり）`
7. 移行先APIで `searchType=plain` の平均探索を行い、平均路線名・方向を取得する
8. 元の定期経路と平均探索結果を比較し、差分を出力する（「移行前後の差分」を参照）
9. 定期経路文字列を組み立て直す（`assignRoute` は方向を挟まずに連結する）
10. 動作確認する。`viaList`・`date` は処理7と同じ値、`searchType=plain`、および入力と同じ形式のパラメーターを指定し、`code=0` かつ `requireUpdate=0` の両方を確認できたら `変換済み`

#### 形式検証

`:` 区切りの要素数を形式ごとの区間長で割った余りが1であること、かつ駅が2個以上であることを確認する。

| 形式                | 区間長 |
| ------------------- | ------ |
| `assignDetailRoute` | 3      |
| `assignRoute`       | 2      |

#### 移行前後の差分

| 列                        | 内容                                                                                                                                            |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `old_route` / `new_route` | 経路を「駅名 →[路線名]→ 駅名」形式に要約した文字列。`old_route` は入力の定期経路文字列から、`new_route` は処理7の平均探索結果から組み立てる |
| `route_changed`           | 経由バス停または平均路線・方向に実質的な変化があれば `変化あり`                                                                                 |

`old_route` は形式検証を通った時点で出力する。`new_route`・`route_changed` は平均探索を行った行にのみ出力する。運賃・所要時間の差分は、移行前の経路を再現できるデータが入力に無いため出力しない。

## 4. CLI仕様

```
python3 -m ekispert_bus_data_migration 入力.csv [入力2.csv ...] [--mapping MAP.csv] [--config PROFILE.json] [--candidates N]
```

- 複数の入力ファイルを同時に指定できる。全ファイルの行をまとめたうえで、機能ごとに該当行だけを処理する
- 結果ファイルの名称と出力先は2.2に従う
- `--mapping` 省略時は、入力CSVと同じフォルダーおよびカレントフォルダー（直下のサブフォルダーを含む）から対応表を自動検出する。検出の対象と、候補が複数見つかった場合の動作は2.1に従う
- `--config` は移行プロファイルを指定する。バス停データ変換だけの場合は省略できる
- `--candidates` は経路シリアライズデータ移行で提示する候補件数（既定3、最大20）。候補件数を増やすと、候補ごとの動作確認のぶんAPI呼び出し回数と実行時間が比例して増える

## 5. 実行環境・制約

- Python 3.11以上。実行時の外部ライブラリは不要（標準ライブラリのみ）
- 実行には対応表（CSV）と入力CSVが必要
- APIを使う機能には「駅すぱあと API」アクセスキーが必要
